
node links to list
list takes node class objects

node creates list item objects

append appeneds items to list in the list class

if next is null, append object to next else run recurse program

if next != null
run recursion on current object
++1 to counter

when next === null
append next and counter



head
next: points to first object
object with 
value: this value
next: points to next object

var is next node object and then check next
if next is not null,
then var is next node object
if next is null
then append new object to next node object